# Loads API keys from a local `.env` file into environment variables.
python-dotenv

# HTTP client used by `mcp_server/weather_api.py` to call OpenWeatherMap.
requests

# Official Model Context Protocol SDK used by `mcp_server/server.py`.
mcp

# LangChain agent framework and OpenAI chat model integration.
langchain
langchain-openai

# Adapter that turns MCP server tools into LangChain tools.
langchain-mcp-adapters
